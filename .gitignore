# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Tools
bin/

.idea

configs/prow/cluster/github-token.yaml
configs/prow/cluster/oauth-token.yaml
configs/prow/cluster/oauth-cookie.yaml
configs/prow/cluster/hmac-token.yaml
configs/prow/cluster/gcs_secrets.yaml

coverage.txt